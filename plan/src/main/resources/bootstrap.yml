server:
  port: 9400
  
spring:
  application:
    name: PlanMS
  cloud:
    consul:
      config:
        fail-fast: false
        enabled: true
        prefix: config
        defaultContext: application
        data-key: data
        profileSeparator: '::'
        format: YAML